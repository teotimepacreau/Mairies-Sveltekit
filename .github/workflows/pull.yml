name: Pull Changes

on:
  push:
    branches:
      - decap

jobs:
  pull:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Pull Changes
      run: git pull origin decap
